# Release v1.4.9 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将所有版本号文件从 1.4.8 同步升级到 1.4.9，打 tag 并推送。

**Architecture:** 直接编辑 4 个版本号文件，提交后打 tag v1.4.9，推送到腾讯 CNB 远程仓库。

**Tech Stack:** Go source, Shell/PowerShell/CMD install scripts, Git

---

## 文件变更清单

| 文件 | 行 | 旧值 | 新值 |
|------|----|------|------|
| `dmxapi-claude-code.go` | 98 | `appVersion = "1.4.8"` | `appVersion = "1.4.9"` |
| `install.sh` | 4 | `VERSION="v1.4.8"` | `VERSION="v1.4.9"` |
| `install.ps1` | 1 | `$VERSION = "v1.4.8"` | `$VERSION = "v1.4.9"` |
| `install.cmd` | 4 | `set VERSION=v1.4.8` | `set VERSION=v1.4.9` |

---

## Chunk 1: 更新版本号并发布

### Task 1: 更新 dmxapi-claude-code.go

**Files:**
- Modify: `dmxapi-claude-code.go:98`

- [ ] **Step 1: 修改 appVersion**

  将第 98 行从：
  ```go
  appVersion = "1.4.8"
  ```
  改为：
  ```go
  appVersion = "1.4.9"
  ```

- [ ] **Step 2: 验证修改**

  Run: `grep -n 'appVersion' dmxapi-claude-code.go | head -3`
  Expected: 第 98 行显示 `"1.4.9"`

---

### Task 2: 更新 install.sh

**Files:**
- Modify: `install.sh:4`

- [ ] **Step 1: 修改 VERSION**

  将第 4 行从：
  ```sh
  VERSION="v1.4.8"
  ```
  改为：
  ```sh
  VERSION="v1.4.9"
  ```

- [ ] **Step 2: 验证修改**

  Run: `head -5 install.sh`
  Expected: 第 4 行显示 `VERSION="v1.4.9"`

---

### Task 3: 更新 install.ps1

**Files:**
- Modify: `install.ps1:1`

- [ ] **Step 1: 修改 $VERSION**

  将第 1 行从：
  ```powershell
  $VERSION = "v1.4.8"
  ```
  改为：
  ```powershell
  $VERSION = "v1.4.9"
  ```

- [ ] **Step 2: 验证修改**

  Run: `head -2 install.ps1`
  Expected: 第 1 行显示 `$VERSION = "v1.4.9"`

---

### Task 4: 更新 install.cmd

**Files:**
- Modify: `install.cmd:4`

- [ ] **Step 1: 修改 VERSION**

  将第 4 行从：
  ```cmd
  set VERSION=v1.4.8
  ```
  改为：
  ```cmd
  set VERSION=v1.4.9
  ```

- [ ] **Step 2: 验证修改**

  Run: `head -5 install.cmd`
  Expected: 第 4 行显示 `set VERSION=v1.4.9`

---

### Task 5: 提交、打 Tag、推送

- [ ] **Step 1: 暂存所有改动**

  ```bash
  git add dmxapi-claude-code.go install.sh install.ps1 install.cmd
  ```

- [ ] **Step 2: 提交**

  ```bash
  git commit -m "chore: bump version to 1.4.9"
  ```
  Expected: 1 commit 成功创建

- [ ] **Step 3: 打 tag**

  ```bash
  git tag v1.4.9
  ```

- [ ] **Step 4: 推送 commit 和 tag**

  ```bash
  git push origin main
  git push origin v1.4.9
  ```
  Expected: tag v1.4.9 出现在远程仓库

- [ ] **Step 5: 确认**

  Run: `git log --oneline -3`
  Expected: 最新 commit 为 `chore: bump version to 1.4.9`
