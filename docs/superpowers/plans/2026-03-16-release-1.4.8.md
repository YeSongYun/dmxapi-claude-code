# Release 1.4.8 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将项目版本号从 1.4.7 升级到 1.4.8，并推送 git tag v1.4.8。

**Architecture:** 修改 4 个文件中的版本号常量/变量，然后提交、打 tag、推送。

**Tech Stack:** Go, Shell, PowerShell, Batch

---

### Task 1: 更新版本号

**Files:**
- Modify: `dmxapi-claude-code.go:98`
- Modify: `install.sh:4`
- Modify: `install.ps1:1`
- Modify: `install.cmd:4`

- [ ] **Step 1: 修改 dmxapi-claude-code.go**

将第 98 行：
```go
appVersion = "1.4.7"
```
改为：
```go
appVersion = "1.4.8"
```

- [ ] **Step 2: 修改 install.sh**

将第 4 行：
```bash
VERSION="v1.4.7"
```
改为：
```bash
VERSION="v1.4.8"
```

- [ ] **Step 3: 修改 install.ps1**

将第 1 行：
```powershell
$VERSION = "v1.4.7"
```
改为：
```powershell
$VERSION = "v1.4.8"
```

- [ ] **Step 4: 修改 install.cmd**

将第 4 行：
```bat
set VERSION=v1.4.7
```
改为：
```bat
set VERSION=v1.4.8
```

- [ ] **Step 5: 验证修改**

```bash
grep -n "1.4.8" dmxapi-claude-code.go install.sh install.ps1 install.cmd
```
预期：4 个文件各有一行匹配。

- [ ] **Step 6: Commit**

```bash
git add dmxapi-claude-code.go install.sh install.ps1 install.cmd
git commit -m "chore: bump version to 1.4.8"
```

---

### Task 2: 打 Tag 并推送

- [ ] **Step 1: 打 tag**

```bash
git tag v1.4.8
```

- [ ] **Step 2: 推送 commits**

```bash
git push
```

- [ ] **Step 3: 推送 tag**

```bash
git push origin v1.4.8
```

- [ ] **Step 4: 验证**

```bash
git log --oneline -3
git tag | tail -5
```
预期：最新 commit 为 `chore: bump version to 1.4.8`，tag `v1.4.8` 存在。
