# Release v1.4.7 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将所有版本号从 1.4.6 升级到 1.4.7，并发布 git tag v1.4.7。

**Architecture:** 依次修改 4 个文件的版本号常量/变量，然后提交并打 tag 推送。

**Tech Stack:** Go, Shell, PowerShell, Batch, Git

---

## Chunk 1: 更新版本号

### Task 1: 更新 Go 源码版本号

**Files:**
- Modify: `dmxapi-claude-code.go:98`

- [ ] **Step 1: 修改 appVersion**

  将第 98 行：
  ```go
  appVersion = "1.4.6"
  ```
  改为：
  ```go
  appVersion = "1.4.7"
  ```

---

### Task 2: 更新 install.sh 版本号

**Files:**
- Modify: `install.sh`（顶部 VERSION 变量）

- [ ] **Step 1: 修改 VERSION**

  将：
  ```sh
  VERSION="v1.4.6"
  ```
  改为：
  ```sh
  VERSION="v1.4.7"
  ```

---

### Task 3: 更新 install.ps1 版本号

**Files:**
- Modify: `install.ps1`（顶部 $VERSION 变量）

- [ ] **Step 1: 修改 $VERSION**

  将：
  ```powershell
  $Version = "v1.4.6"
  ```
  改为：
  ```powershell
  $Version = "v1.4.7"
  ```

---

### Task 4: 更新 install.cmd 版本号

**Files:**
- Modify: `install.cmd`（顶部 VERSION 变量）

- [ ] **Step 1: 修改 VERSION**

  将：
  ```bat
  set VERSION=v1.4.6
  ```
  改为：
  ```bat
  set VERSION=v1.4.7
  ```

---

## Chunk 2: 提交并发布 Tag

### Task 5: 提交版本号变更

- [ ] **Step 1: 确认修改正确**

  ```bash
  grep -n "1.4.7" dmxapi-claude-code.go install.sh install.ps1 install.cmd
  ```
  预期：4 个文件均出现 `1.4.7`

- [ ] **Step 2: 提交**

  ```bash
  git add dmxapi-claude-code.go install.sh install.ps1 install.cmd
  git commit -m "chore: bump version to 1.4.7"
  ```

---

### Task 6: 打 Tag 并推送

- [ ] **Step 1: 创建 tag**

  ```bash
  git tag v1.4.7
  ```

- [ ] **Step 2: 推送代码和 tag**

  ```bash
  git push
  git push origin v1.4.7
  ```

  预期：推送成功，远程仓库出现 `v1.4.7` tag。
